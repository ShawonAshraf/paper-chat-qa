[tool.poetry]
name = "retriever"
version = "0.1.0"
description = ""
authors = ["Shawon Ashraf <shawon13@live.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
